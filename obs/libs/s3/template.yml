bucket:
  put_bucket_acl:
    parameters:
      Bucket:
        type: string
        label: Bucket Name
        description:
        default:
      ACL:
        type: string
        label: ACL
        description: Access Control for Bucket
        default: public-read
  put_bucket_accelerate_configuration:
    parameters:
      Bucket:
        type: string
        label: Bucket Name
        description:
        default:
      AccelerateConfiguration:
        type: array
        label: Accelerate Configuration
        description:
        default:
          Status: Enabled
  list_buckets:
    parameters:
object:
  put_object:
    parameters:
      Bucket:
        type: string
        label: Bucket Name
        description:
        default:
      Key:
        type: string
        label: Key Name
        description: Object file name
        default:
      Body:
        type: file
        label: Body
        description: Body file
        default:
  put_object_acl:
    parameters:
      ACL:
        type: string
        label: Bucket Name
        description:
        default:
      Bucket:
        type: string
        label: Bucket Name
        description:
        default:
      Key:
        type: string
        label: Key Name
        description: Object file name
        default:
      Body:
        type: file
        label: Body
        description: Body file
        default:
  list_objects:
    parameters:
      Bucket:
        type: string
        label: Bucket Name
        description:
        default: